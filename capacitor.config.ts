import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arcanys.fooionic',
  appName: 'fooionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
